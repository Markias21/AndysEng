CLAUDE.md

프로젝트 개요

영어 학습 앱. 핵심 도메인: 어휘 학습(vocabulary), 간격 반복 복습(review), 퀴즈(quiz), 학습 진도(progress).
사용자 가치의 중심은 복습 스케줄링과 학습 데이터의 정확성이다. 이 부분의 코드 품질을 최우선으로 한다.
이 프로그램은 회화를 배우긴 하나 토플 공부를 위해 만들어졌다. 예문을 만들거나 공부 방향을 짤 때 유의할 것
토플 문제 형식과 공부에 대한 아이디어의 경우 https://blog.naver.com/messijessi/224253337626를 참고한다.

기술 스택

- 정적 PWA (서버 없음, 의존성 0개). public/이 앱 전부이며 GitHub Pages 등 정적 호스팅에 배포한다.
- 빌드 스텝 없는 vanilla JS (브라우저 네이티브 ES 모듈). 개발 서버는 dev-server.js (node 내장 http).
- AI: 브라우저에서 Anthropic REST API를 fetch로 직접 호출, 모델 claude-sonnet-5 (public/js/shared/claude.js 경계 뒤에만 존재)
- 로그인 정보(닉네임·GitHub PAT·Claude 키): 비밀번호로 암호화(PBKDF2→AES-GCM)해 localStorage 보관 (public/js/shared/keyvault.js)
- 저장소: localStorage 단일 JSON (public/js/shared/store.js). 리포트는 File System Access API로 로컬 폴더 저장 (public/js/shared/localfs.js). 학습 기록은 GitHub Contents API로 공유 프라이빗 레포에 수동 저장/불러오기 가능 (public/js/shared/github.js)

자주 쓰는 명령어

npm run dev        # 개발 서버 (http://localhost:3000)
npm test           # 전체 테스트 (node --test)

핵심 원칙 (모든 코드 작성 시 적용)

1. 의존성 방향


도메인 로직(복습 알고리즘, 채점, 진도 계산)은 프레임워크, DB, UI를 절대 import하지 않는다.
의존 방향은 항상: UI/API → 애플리케이션 로직 → 도메인. 역방향 금지.
외부 서비스(TTS, 사전 API 등)는 인터페이스 뒤에 두고, 도메인은 인터페이스만 안다.


2. YAGNI — 미리 만들지 않기


현재 요구사항에 없는 추상화, 설정 옵션, 제네릭 구조를 만들지 않는다.
구현체가 하나뿐인 인터페이스는 만들지 않는다. (외부 서비스 경계는 예외)
"나중에 필요할 것 같다"는 이유로 코드를 추가하려면 먼저 사용자에게 물어본다.


3. 기능 단위 구조


폴더는 레이어가 아니라 기능(feature) 기준으로 나눈다.

좋음: features/vocabulary/, features/review/, features/quiz/
피함: 최상위 controllers/, services/, utils/ 로만 나누는 구조



한 기능의 코드는 한 폴더 안에서 완결되게 한다. 기능 간 공유는 shared/로 명시적으로만.


4. 크기와 복잡도 제한


함수는 한 가지 일만 한다. 함수 이름에 "and"가 들어가면 분리를 검토한다.
파일이 300줄을 넘으면 분리를 검토하고, 분리 계획을 먼저 제안한다.
조건문 중첩이 3단계를 넘으면 early return 또는 함수 추출로 평탄화한다.


5. 테스트


도메인 로직(복습 스케줄, 채점, 진도 계산)은 반드시 단위 테스트를 함께 작성한다.
테스트는 구현이 아니라 동작을 검증한다. 리팩터링해도 깨지지 않는 테스트를 지향.
버그 수정 시: 버그를 재현하는 테스트를 먼저 추가하고 수정한다.


6. 네이밍과 도메인 용어


코드에서 도메인 용어를 통일한다: word(단어), deck(단어장), card(복습 카드), reviewSession(복습 세션), interval(복습 간격), streak(연속 정답/연속 학습일), due(다음 복습 시각). 동의어 혼용 금지.
새 도메인 용어가 필요하면 이 섹션에 추가한다.

7. 작업 방식


큰 변경(새 기능, 구조 변경) 전에는 계획을 먼저 제시하고 확인받는다. 확실하지 않다면 반드시 유저에게 실시간으로 질문하면서 정답을 찾아가라.
기존 코드 스타일과 패턴을 따른다. 새 패턴 도입은 이유와 함께 제안 먼저.
에러는 삼키지 않는다. 처리하거나 명시적으로 전파한다.
주석은 "무엇"이 아니라 "왜"를 설명할 때만 쓴다.
코드를 작성하고 나면 AI는 커밋을 메시지와 함께 진행한다.
반드시 한글로 개발자에게 코드를 수정한 것에 대해 설명한다.
같은 성능에서 토큰 사용량을 줄일 수 있는 방법을 찾아 적용한다.

8. 하지 말 것


요청하지 않은 라이브러리 추가 금지. 필요하면 이유와 함께 제안.
테스트 없이 도메인 로직 변경 금지.
죽은 코드, 주석 처리된 코드 남기기 금지.


아키텍처 결정 기록 (ADR)

결정이 생길 때마다 아래 형식으로 추가한다. Claude는 여기 기록된 결정을 뒤집는 제안을 하기 전에 반드시 해당 항목을 언급한다.

날짜결정이유YYYY-MM-DD(예: SRS 알고리즘은 SM-2 변형 사용)(예: 구현 단순, 검증된 알고리즘)

- 2026-07-17 | 멀티유저 도입, store 스키마에 `users`/`sessions` 차원 추가 (기록을 사용자별 분리) | 구글 로그인 기반 개인별 학습 기록·드라이브 저장 요구.
- 2026-07-17 | 세션은 외부 라이브러리 없이 랜덤 불투명 토큰 + HttpOnly 쿠키(`shared/session.js`) | 의존성 최소화, YAGNI.
- 2026-07-17 | 구글 인증은 `google-auth-library`, 드라이브는 REST를 fetch로 직접 호출(`shared/drive.js`) | OAuth·토큰갱신은 라이브러리에 맡기고 드라이브는 의존성 최소화.
- 2026-07-17 | 드라이브 접근에 전체 `drive` 스코프 사용 | AndysNote 폴더를 이름으로 검색·이동하려면 `drive.file`로는 부족.
- 2026-07-17 | **정적 PWA 전환** — Express 서버·구글 로그인·드라이브·멀티유저를 모두 제거 (위 4개 ADR을 대체) | GitHub Pages 등 정적 호스팅 배포 및 개인 로컬 실행 요구. 서버 유지 비용 없음.
- 2026-07-17 | API 키는 사용자 개별 키를 브라우저에서 직접 사용 (`anthropic-dangerous-direct-browser-access`), PBKDF2(310k)→AES-GCM으로 암호화해 localStorage 보관, 비밀번호 입력 시에만 메모리 복호화 | 서버 없이 쓰되 평문 저장 금지. CSP meta + 외부 스크립트 0개로 XSS 표면 최소화.
- 2026-07-17 | 학습 기록은 localStorage 단일 JSON(`andyseng:data`), 리포트는 File System Access API로 사용자가 고른 폴더의 `AndysEng/`에 md+json 저장, 미지원 브라우저는 다운로드 폴백 | 서버·클라우드 없이 데이터 소유권을 사용자에게.
- 2026-07-17 | SRS 알고리즘은 고정 간격 사다리 SM-2 변형: 정답 시 1→3→7→14→30→60→120일, 오답 시 처음으로(10분 뒤 재도전) | 구현 단순, 검증된 망각곡선 접근. ease factor는 YAGNI로 보류.
- 2026-07-17 | 복습 퀴즈(빈칸 생성·채점)·통계·리포트는 AI를 쓰지 않고, 표현 공부는 호출 1회에 5개를 받아 큐에 쌓는다 | AI 사용량 최소화 요구.
- 2026-07-17 | 친구 그룹 공유 지원 — 로그인 시 닉네임·GitHub PAT·Claude 키 3개를 함께 암호화 저장(`keyvault.js` 페이로드를 JSON으로 일반화), 학습 기록을 공유 프라이빗 레포(`Markias21/AndysEng_log`, `shared/github.js`에 상수로 고정)의 `data/<nickname>/andyseng-data.json`에 수동 저장/불러오기 버튼(자동 동기화 없음)으로 push/pull. GitHub PAT는 신뢰된 친구들끼리 공유하며 앱 차원의 권한 격리는 하지 않음(닉네임 폴더 분리는 관례일 뿐 GitHub 권한으로 강제되지 않음) — 바로 위 "정적 PWA 전환" ADR의 "멀티유저 제거" 결정을 부분적으로 재개함 | 소수의 신뢰된 친구들과 서버 없이 데이터를 나눠 쓰고 싶다는 요구. 별도 백엔드·인증 시스템 없이 GitHub Contents API를 REST로 직접 호출해 의존성 0개 유지.
- 2026-07-17 | 코드스페이스 개발 환경에서는 `.env`(gitignored)의 `ANTHROPIC_API_KEY`/`GITHUB_PAT_API_KEY`를 `dev-server.js`가 읽어 `/__dev/session`으로 내려주고, 닉네임 "Andy"로 게이트를 건너뛴다 | 개발 편의. 이 라우트는 dev-server.js에만 존재해 정적 배포(GitHub Pages)에는 영향 없음 — 프로덕션 비밀번호 게이트는 그대로 유지.
- 2026-07-18 | **토큰 효율화 — 콘텐츠 생성을 로컬 정적 데이터로 이동.** 회화 주제·오프닝(`features/conversation/topics.js` 100개), 글쓰기 질문(`features/writing/prompts.js` 100개), 표현(`features/expression/*-expressions.js`)을 앱에 정적으로 포함하고, AI 호출은 **첨삭/채점(chatJSON)만** 남긴다. 이로써 `claude.js`의 `chatText`(자유 텍스트 오프닝/질문 생성용)를 제거함 — 위 "복습 퀴즈…표현 공부는 호출 1회에 5개를 받아 큐에 쌓는다" ADR의 표현 큐 방식을 대체(exprQueue 및 store의 큐 함수 제거) | AI 사용량을 한 번 더 줄이라는 요구. 미리 가질 수 있는 데이터는 로컬에 둔다.
- 2026-07-18 | 표현은 **컬렉션을 분리**한다: 회화 표현 300 + 토플 표현 300 (각각 별도 파일). 유저가 회화/토플 중 골라 학습하고 기록에 `collection`을 남긴다. AI는 유저가 쓴 예문 첨삭만 | 회화·학술 표현의 성격이 달라 별도 모음으로 관리(겹침 허용). 정적 데이터라 확장 가능(현재 각 300개, 목표 1000개까지 동일 포맷으로 append).
- 2026-07-18 | 로컬 데이터에서 항목을 고를 때 **최근 사용 항목 회피**: 기록(sessions/writing/expression)의 최근 20개에 없는 후보를 우선 뽑는 순수 함수 `shared/pick.js`(`pickFresh`) 사용, 후보가 없으면 전체에서 선택 | 새로고침해도 같은 주제/질문이 반복된다는 불편 해소.
- 2026-07-18 | 회화는 시작 시 **영어 배경(scene) 설명**을 먼저 보여 주고(파트너 대사는 그 상황에 맞춰 로컬 오프닝), 시스템 프롬프트에 scene을 넣어 대화가 상황을 유지하게 한다 | 상황을 이해하고 말하도록 돕는 요구.
- 2026-07-18 | 회화 UI에서 유저 발화 한 턴을 `.turn.user-turn`으로 감싸고, **데스크탑에서는 피드백을 말풍선 왼쪽(빈 공간)에** CSS `order`로 배치, 모바일(≤720px)은 아래로 스택 | 데스크탑 채팅 느낌 강화 요구.
- 2026-07-18 | **점수 시스템 — 요소별 S/A/B/C/F 등급, 점수는 로컬 산출.** AI는 배점 요소마다 등급(S~F)만 매기고(스키마 enum 한 글자), 점수는 `shared/scoring.js`(순수 함수)에서 등급을 그 요소 만점 비율로 환산한다: S=×1.0, A=×0.8, B=×0.6, C=×0.4, F=×0.2 (1/5 간격). 요소 배점 합은 세 기능 모두 100점이라 총점은 0~100 유지(기존 통계/평균 호환). 종합 등급은 총점 비율을 0.2 간격의 중간값(0.9/0.7/0.5/0.3) 경계로 환산. 배점: 회화 자연스러움30·문법성40·구조30, 글쓰기 구조20·문법30·이해30·수식어자연스러움20, 표현 자연스러움50·문법30·이해20. 기록에 `grades`(요소 등급)+`score`(총점) 저장, 피드백에 요소별 점수·등급 분해 표시, 각 intro에 배점 안내(`rubricGuideHTML`) 노출 | AI가 숫자를 매기는 것보다 등급 한 글자가 토큰이 적고 일관적. 점수 산출 규칙을 코드로 고정해 검증 가능하게(scoring.test.js).
- 2026-07-18 | **학습 달력(통계).** `stats.js`에 순수 함수 `calendarMonth(daily, year, month)`로 월별 격자를 만들고, `dailyStats`에 기능별 일평균(`convAvg`/`writeAvg`/`exprAvg`) 추가. 달력 칸 배경은 그날 평균 점수의 종합 등급 색(S~F), 칸 안에 💬회화 ✍️글쓰기 💡표현 평균을 표기, 월 이동 버튼 제공 | 말해보카 등 학습앱 달력 참고. 그날의 학습 밀도·점수를 한눈에.
- 2026-07-19 | **복습을 AI 문장 채점으로 전환 — "복습 퀴즈는 AI 미사용/100% 로컬 무료" ADR(2026-07-17)을 뒤집음.** 복습은 이제 표현만 보여 주고(뜻은 `뜻 보기` 버튼으로만 공개) 유저가 문장을 만들면 `chatJSON`으로 표현 루브릭(자연스러움/문법/이해) S~F 채점. 총점 `REVIEW_PASS_SCORE`(50, B 경계) 이상이면 간격 사다리 상승, 미만이면 처음으로(`scoring.reviewPassed`, 순수 함수+테스트). 빈칸 퀴즈 도메인(`srs/cloze.js`+테스트)은 제거 | 회화·글쓰기에서 배운 표현을 실제로 산출(produce)하며 복습하도록. 토큰 증가는 감수(유저 결정).
- 2026-07-19 | **표현 영역 제거, 복습이 대체.** 프론트에서 `💡 표현` 탭·`#view-expression`을 없애고 `app.js`에서 `expression.init()` 미호출. 단 표현 데이터·코드(`features/expression/*`, `*-expressions.js`)는 **삭제하지 않고 보존**(향후 재사용 가능성, 유저 지시). 앱 시작 시 `store.purgeExpressionCards()`로 옛 표현 학습에서 쌓인 덱 카드(`source`가 `expression`으로 시작)를 1회 정리. 복습 덱은 회화·글쓰기 표현만 채운다 | 표현을 "미리 주는 것"이 아니라 회화·글쓰기에서 나온 표현을 복습하는 방향으로.
- 2026-07-19 | **CEFR 레벨(A1~C2) 도입 — 한 곳에서 설정해 회화·글쓰기·복습에 공통 적용.** `shared/levels.js`(순수 도메인: `filterByLevel` ±span, `WRITING_TIPS`, `CONV_GUIDANCE`)+테스트, `store.profile`(`level` 기본 B1, `exprPerConv` 기본 2). 회화는 시스템 프롬프트에 레벨 가이드 주입, 글쓰기는 레벨별 목표 팁 노출+`cefr_level`로 글 품질 레벨 채점, 복습은 내 레벨 ±1 표현만 출제. 표현 난이도는 회화·글쓰기 표현 추출 시 AI가 CEFR `level`을 함께 매겨 덱에 저장 | 레벨에 맞는 공부 방향 제시(기능 6). 모든 표현에 등급을 매기지 않고, 회화·글쓰기에서 나온 표현만 난이도 등급을 매겨 복습에 활용.
- 2026-07-19 | **회화 표현 수집은 주기적(기본 3턴마다 1회) + 개수 조정 가능(1~3).** `EXTRACT_EVERY=3` 턴마다 유저 설정 `exprPerConv`(1~3)개의 표현을 뽑아 덱에 추가. 추출하지 않는 턴은 응답 스키마에서 `expressions` 필드를 아예 빼 토큰을 아낀다(`buildReplySchema`) | 매 턴 추출은 토큰 낭비. 주기·개수로 사용량 조절.
- 2026-07-19 | **스킵 기능.** 회화·글쓰기에 `🔄 다른 주제/질문` 리롤 버튼(원치 않거나 중복 주제 회피), 복습에 `건너뛰기 →`(채점·SRS 변화 없이 카드를 세션 뒤로) 추가 | 기능 7.
- 2026-07-19 | AI 모델을 `claude-opus-4-8` → `claude-sonnet-5`로 변경 | 비용 절감. 문법/구조 채점·CEFR 판정 등 루브릭 기반 등급 매기기에는 소넷으로 충분하다고 판단.
- 2026-07-19 | **서비스 워커를 캐시 우선 → 네트워크 우선(오프라인만 캐시 폴백)으로 변경**, `sw.js` 등록에 `updateViaCache: "none"` 추가. GitHub Pages 배포 직후 새로고침해도 게이트 화면이 구버전(설정 버튼 등 새 UI 요소 누락)으로 떠 아무 반응이 없어 보이는 버그를 Playwright로 재현해 원인 확정: GitHub Pages가 `sw.js`에 `max-age=600` 캐시 헤더를 붙여, 브라우저가 그 시간 동안 새 서비스워커를 감지하지 못하고 캐시 우선 전략이 배포 후 최소 한 번은 구버전을 서빙했다 | 오프라인 지원은 유지하되, 온라인일 때는 배포 즉시 최신 코드가 반영되도록.
- 2026-07-19 | **회화 채팅에 프롬프트 캐싱 적용.** `features/conversation/ui.js`에서 히스토리를 문자열로 뭉쳐 매 턴 새 유저 메시지로 보내던 방식을 멀티턴 `messages` 배열로 바꾸고, 직전까지의 마지막 메시지에 `cache_control: {type: "ephemeral"}`을 찍어 이후 턴은 그 지점까지 캐시에서 읽는다. 이를 위해 `tutorSystem`에서 표현 추출 턴마다 달라지던 지시문을 시스템 프롬프트(캐시 prefix)에서 빼고, 턴마다 바뀌는 유저 메시지 쪽(원래도 캐시되지 않는 휘발성 부분)으로 옮겨 시스템 프롬프트를 턴 전체에서 완전히 고정시켰다 | 회화가 길어질수록 매 턴 전체 히스토리를 정가로 재처리해 입력 토큰 비용이 턴 수의 제곱에 비례해 커지는 문제 해결. 요약/절단 대신 캐싱을 택한 이유는 맥락(scene 일관성) 손실 없이 동일한 비용 절감 효과를 얻을 수 있어서.
- 2026-07-19 | **사전 + 단어장 도입.** 회화·글쓰기 툴바의 `📖 사전` 버튼으로 모달(`#dict-modal`)을 열어 단어를 조회한다. **양방향**: 한글 질의는 한→영(알맞은 영단어들), 영어 질의는 영→한(뜻 조회). 방향 판정은 순수 함수 `features/dictionary/detect.js`(`detectDirection`/`cacheKey`)+테스트. 결과는 `entries:[{word,phonetic,pos,meaning,example}]`로 **뜻이 크게 다른 범주만 분리해 각 범주 예문 1개**(make→만들다/~하게 하다). 사전 AI 호출은 **Haiku 고정**(`chatJSON`에 `modelOverride` 파라미터 추가, 설정 모델과 무관)이고, 결과는 **영구 캐시**(`store.dict[cacheKey]`)라 같은 질의는 평생 1회만 토큰을 쓴다(백업·GitHub 동기화에 함께 저장). 각 항목을 `➕`로 단어장(`store.words`)에 개별 저장(중복은 `word+meaning`). 단어장은 새 상단 탭 `📖 단어장`(`features/words/ui.js`)의 **플래시카드**: 단어+예문을 보고 `뜻 보기` → `🙂 기억했어요/😵 까먹었어요` **자가평가**로 SRS를 진전(간격 스케줄링은 `srs/scheduler.js` 재사용, AI 채점 없음). 단어장·복습 목록에서 `🗑`로 카드 빼기(`store.removeWord`/`removeCard`). store 스키마 `version 3`(`words:[]`, `dict:{}` 마이그레이션) | 무료 사전 API는 영어 뜻만·과다 나열이라 말해보카식 한국어 뜻 구분이 불가. 파파고/케임브리지는 서버·키·CORS 문제로 정적 PWA에 부적합. Claude+캐시로 서버 없이 원하는 경험을 얻고, Haiku·캐시로 토큰을 최소화.
- 2026-07-19 | **표현 복습을 버튼 기반 SRS로 개편 — 위 "AI 점수(REVIEW_PASS_SCORE)로 통과 판정" ADR(2026-07-19)을 수정.** 표현만 보여 주고 먼저 `🙂 기억나요/😵 까먹었어요`로 자가평가한다. **까먹었어요**: 뜻+예시 예문을 공개한 뒤 문장 작성 → 채점 후 다음 복습은 처음으로. **기억나요**: 뜻·예시를 숨긴 채 문장 작성 → 채점 후 `😵 잘못 생각했어요/다음 →`로 최종 확인('다음'=정말 기억→간격 상승, '잘못 생각했어요'=처음으로). **다음 복습 시점은 AI 점수가 아니라 이 버튼이 결정**하고, `chatJSON` 채점은 첨삭·원어민 문장·등급 등 피드백 전용으로 강등. 이에 따라 `scoring.reviewPassed`/`REVIEW_PASS_SCORE`(및 그 테스트) 제거 | 회화·글쓰기에서 배운 표현을 "기억하는지 먼저 스스로 시험하고 산출하며" 복습하도록. 통과 여부를 점수로 강제하기보다 학습자의 자기 인지에 맡기는 편이 망각곡선 취지에 맞음(유저 결정).
- 2026-07-19 | **표현 복습과 단어장을 하나의 `🔁 복습` 탭으로 통합 — 위 "사전 + 단어장 도입" ADR(2026-07-19)의 별도 `📖 단어장` 탭·`features/words/ui.js`를 대체.** `features/srs/ui.js`가 `store.deck`(표현 카드)와 `store.words`(단어 카드)를 함께 큐로 묶어 다룬다(각 카드를 `{kind:"expression"|"word", raw, term, ...}`로 감싸는 어댑터, store 스키마는 그대로 유지). 흐름은 항상 먼저 `🙂 기억나요/😵 까먹었어요` 자가평가 → 뜻+예시 공개 → **그 다음이 선택**: `✍️ 예문 만들기`(AI 첨삭, 기억나요였다면 `잘못 생각했어요/다음 →`로 최종 확인 — 기존과 동일)이거나 `⏭ 예문 없이 넘어가기`(AI 호출 없이 방금 한 자가평가를 그대로 다음 간격 결정에 반영). 이에 따라 예문 작성 도중에 있던 세션 뒤로 미루기용 `건너뛰기 →` 버튼(위 "스킵 기능" ADR)은 이 새 선택 단계로 대체되어 제거했다. `features/words/ui.js` 삭제, `index.html`·`app.js`·`sw.js` 프리캐시 목록에서 `📖 단어장` 탭·`words.render()` 제거 | 표현과 단어 모두 "짧은 텍스트를 기억하는지 확인하고 필요하면 산출까지" 하는 동일한 복습 경험이라 화면을 나눌 이유가 없었다(유저 결정). 예문 만들기를 강제하지 않고 선택으로 바꿔, 단어장 특유의 "가볍게 인지만 하고 넘어가기"와 표현 복습의 "산출 검증"을 모두 지원.- 2026-07-19 | **글쓰기 첨삭 개편.** (1) 오타·대소문자는 채점에서 완전히 제외 — 별도 `spelling` 필드(원문→교정만, 이유 설명 없음)로 분리하고 `corrections`·`grammar` 등급은 이를 다루지 않는다. (2) `corrected_answer`/`native_answer`를 문자열에서 **문장 단위 배열** `[{sentence, translation}]`로 바꿔 한 문장씩 줄바꿈하고 문장마다 한국어 해석을 붙인다. (3) 원어민 모범 답안을 유저 CEFR 레벨(`profile.level`)에 맞춰 작성하도록 지시. (4) 고친 부분·새 중요 표현은 AI가 `[[...]]`로 감싸고 `dom.underlineHTML`이 이스케이프 **후** 마커만 `<u class="mark">`로 치환해 밑줄 렌더(XSS 안전). (5) 표현은 자동으로 덱에 넣지 않고 **사전과 같은 개별 `➕` 버튼**으로 유저가 고른 것만 추가 — 회화도 동일(`addToDeck` 자동 호출 제거, 피드백 안에 버튼 노출). 공용 헬퍼 `spellingHTML`/`sentenceLinesHTML`/`expressionAddHTML`/`wireExpressionAdds`를 `shared/dom.js`에 둔다. 기존 기록(문자열 답안)은 `sentenceLinesHTML`·리포트 `answerLines`가 문자열도 받아 그대로 표시 | 오타 감점이 실력 평가를 왜곡하고, 통짜 문단은 어디가 왜 고쳐졌는지 보이지 않으며, 관심 없는 표현까지 복습 덱에 쌓이던 문제 해소(유저 요구).
- 2026-07-21 | **글쓰기·회화 자동 GitHub 저장 도입.** `shared/autosave.js`(`autoSaveToGithub`)를 새로 두고, 글쓰기는 첨삭 완료 직후, 회화는 `다른 주제`로 전환하는 순간(진행 중이던 대화가 있을 때만) 자동 호출한다. 실패해도 조용히 토스트만 띄우고 학습 흐름은 막지 않는다. 설정 탭의 기존 수동 저장/불러오기 버튼(`features/sync/ui.js`)은 그대로 유지 | 저장을 깜빡해 학습 기록을 잃는 일을 줄이라는 요구. 수동 저장은 여전히 유효한 안전판이라 제거하지 않음.
- 2026-07-21 | **글쓰기 "🧱 구조 제시" 버튼 추가.** `features/writing/structure.js`에 논설문 5단 구조 템플릿(도입→이유1→이유2→반론(선택)→결론)과 신호어(signal word) 표현 45개를 정적으로 둔다(AI 호출 없음). 버튼을 누르면 템플릿 + 무작위 5개 표현을 보여주고, `🔄 다른 표현`으로 재추출한다(`shared/pick.js`의 순수 함수 `sampleN` 추가). 각 표현은 기존 `➕ 복습에 추가`로 개별 담기 가능 | 예문 없이 첨삭만으로는 구조를 배우기 어렵다는 요구. 이미 갖고 있던 신호어 목록이라 AI 호출 없이 정적 데이터로 충분(YAGNI).
- 2026-07-21 | **번역기(한→영) 도입 + 사용 시 감점.** 회화·글쓰기 툴바의 `🌐 번역기` 버튼으로 공용 모달(`features/translate/ui.js`, `shared/translate.js`)을 연다. 사전과 동일하게 **Haiku 고정**이지만 자유 문장이라 재사용 가치가 낮아 **캐시는 하지 않는다**. 번역기를 쓸 때마다 `shared/translate.js`의 기능별 카운터에 기록해 두고, 그 기능의 다음 판정 시점(글쓰기 제출/회화 턴 전송)에 꺼내 쓰면서 즉시 0으로 되돌린다: 글쓰기는 문장당 **-5점**, 회화는 **-10점**(`TRANSLATOR_PENALTY`), 채점된 원점수에서 차감해 0 미만은 0으로 clamp. 배점 요소별 등급(`scoreBreakdownHTML`)은 그대로 두고, 그 아래 별도 줄(`dom.translatorPenaltyHTML`)로 "번역기 N회 사용 → -X점 → 최종 Y점"을 보여준다. 질문/주제를 바꾸면 그 사이 누적된 사용 횟수는 버리고 새로 시작한다 | 번역기에 의존하지 않고 스스로 영작하도록 유도하되, 막히면 참고는 할 수 있게 하라는 유저 요구.
- 2026-07-22 | **글쓰기 첨삭에 후속 질문하기(Q&A) 추가.** 첨삭 결과 카드 아래에 `writing-qna-form`을 두고, 질문을 보내면 `chatJSON`(스키마 `{answer:string}`, 자유 텍스트 `chatText`는 2026-07-18 ADR로 이미 제거됨)으로 한국어 답변만 받는다. 첫 질문에는 그 첨삭 세션의 질문·학생 답안·첨삭 결과 JSON을 컨텍스트로 함께 보내고, 이후 질문은 `qnaHistory`(모델에 그대로 replay할 user/assistant 메시지 배열)에 이어 붙여 같은 세션 안에서 멀티턴으로 이해한다. 새 질문을 받거나 다시 첨삭받으면 `resetQna()`로 컨텍스트·기록을 버린다(세션 간 컨텍스트를 들고 가지 않음). store에는 저장하지 않는 화면 전용 상태 | 학생이 왜 이렇게 고쳐졌는지 그 자리에서 되물을 수 있어야 한다는 요구. 표현 추출처럼 정적으로 대체할 수 없는 자유 질문이라 AI 호출이 불가피하지만, 스키마를 강제해 응답을 `answer` 한 필드로 좁혀 토큰을 아꼈다.
- 2026-07-22 | **회화를 8개 카테고리(대학생활/연애/가치관/공부/직업·미래/상담/취미/기타)로 분류.** `features/conversation/categories.js`(순수 로직: 카테고리 정의·주제 풀 매핑, UI/저장소 미import)가 카테고리별 주제 풀을 골라준다. 기존 100개 주제는 `etcTopics`(기타)로 남기고, 대학생활·가치관·공부·직업·취미 주제를 새로 추가했다(`topics.js`, `hobby-topics.js`). 취미는 음악/스포츠/게임/영화·드라마/독서/요리/미술/사진/여행/반려동물/아웃도어 11개 하위 카테고리 + 자율(파트너 오프닝 없이 플레이어가 먼저 시작)로 세분화. 인트로 화면에 카테고리 그리드를 두고, 대화 중에도 `🗂 다른 카테고리` 버튼으로 언제든 처음 화면으로 돌아갈 수 있다(이전엔 새로고침 전까지 불가능했음) | 회화 주제를 상황별로 골라 연습하고 싶다는 요구, 취미는 넓은 범위를 다루고 싶다는 요구.
- 2026-07-22 | **연애 카테고리 — 성별 기반 고정 페르소나 + 관계 기억.** `shared/personas.js`(정적 데이터, conversation과 settings 양쪽에서 써서 shared/에 둠)에 여성 5·남성 5, 총 10개 페르소나를 안정형→다정형→애정형→불안형→멘헤라형 5단계로 성격·취향·취미·장단점까지 구체적으로 정의. 설정에서 내 성별(`profile.gender`)과 반대 성별 중 상대 하나(`profile.romancePartnerId`)를 고정해 계속 같은 캐릭터와 대화한다. 대화가 끝날 때(카테고리 이탈/새로고침) Haiku로 대화를 압축 요약해 `store.romanceMemory[partnerId]`에 **교체 저장**(누적 append가 아님, 토큰 무한 증가 방지)하고 다음 대화 시작 시 시스템 프롬프트에 주입해 상대가 이전 대화를 "기억"하게 한다. 요약은 논블로킹으로 실행 — 새로고침 화면 전환은 즉시 이뤄지고, 요약이 도착하면 같은 상대와의 세션이 계속되고 있을 때만 그 자리에서 memory를 갱신해 다음 턴부터 반영한다. 연애의 "다른 주제" 버튼은 상대가 고정이라 실제 동작(같은 상대와 다시 시작)에 맞춰 "🔄 새로고침"으로 표기 | 이성친구 역할극을 하려면 성별에 맞는 상대·뚜렷한 성격 스펙트럼이 필요하다는 요구, 대화가 이어질수록 "기억하는 사이"처럼 느껴지길 바라는 요구를 토큰 비용을 늘리지 않는 선에서 구현.
- 2026-07-22 | **상담 카테고리.** `shared/personas.js`의 `counselPersona`(밝고 적극적인 상담사 Grace, 상황 3종 로테이션)가 학습자의 고민(이성·인간관계 등)을 들어주는 역할을 한다. 연애처럼 상대를 고정하진 않고 상황만 최근 미사용 것으로 순환(`pickFresh`) | 교수·멘토 같은 상담자 역할극으로 고민을 말해보는 연습이 필요하다는 요구.
- 2026-07-27 | **채점 재설계 — CEFR 4대 절대 기준 + 9단계 등급 + 진도 축 통합.** 세 가지를 바꿨다. (1) **기준 재설계:** 회화·글쓰기·복습이 겹치던 요소(자연스러움/문법/이해/구조 등) 대신 서로 독립적으로 움직이는 CEFR/IELTS식 4대 축을 공유한다 — `task`(내용·과제 수행)·`accuracy`(정확성)·`range`(어휘·구문 다양성)·`fluency`(유창성·자연스러움). 배점만 기능별로 다르고 합은 100 유지. 절대 밴드는 `shared/scoring.js`에 `TASK_RUBRIC`/`RANGE_RUBRIC`/`GRADE_SCALE_NOTE` 상수로 중앙화하고 `accuracy`=`GRAMMAR_RUBRIC`, `fluency`=`NATURALNESS_NOTE` 재사용(드리프트 방지). (2) **절대 채점 + 세분화:** 채점 바를 학습자 레벨과 무관한 고정 CEFR 절대 기준으로 바꿔(AI 파트너 난이도·모범답안 레벨만 상대 유지) 실력이 늘면 점수가 실제로 오르게 했다. 등급은 F를 제외한 각 등급에 `+`를 더해 9단계로 세분화(`S+ S A+ A B+ B C+ C F`), 비율은 0.1 간격(S+ 1.0 … F 0.2 바닥). `overallGrade`·`OVERALL_THRESHOLDS`도 9단계로. 배지 클래스에 `+`를 못 쓰므로 `gradeClass`(“S+”→`grade-Splus`) 추가, 달력 히트맵은 `+`를 떼어 5색 계열로만 칠함. (3) **신호 통합:** 100점 루브릭 총점을 대표 진도 지표로 삼고, 발화별 CEFR 레벨·글쓰기 TOEFL 밴드(0~4)는 같은 절대 프레임에서 나온 보조 라벨로 둔다. temperature 0 일관성 결정은 유지(점수 변동은 무작위가 아니라 절대 척도·세분화·구체 앵커에서 나옴). **대체·수정하는 ADR:** 2026-07-18 “등급 한 글자(S/A/B/C/F)”→9단계로 세분화(글자·로컬 산출 원칙은 유지), 2026-07-18 배점 요소 정의→4대 축으로 교체. 보존된 미사용 `features/expression/ui.js`(2026-07-19 ADR)는 손대지 않아 옛 grades 키를 그대로 둔다(비실행) | 채점 요소가 사실상 한 판단의 반복이라 독립적이지 않고, 5단계+레벨 상대 채점 탓에 실력이 늘어도 늘 같은 점수가 나온다는 유저 불만 해소.
- 2026-07-29 | **원어민 표현을 "읽기"에서 "인출"로 — 글쓰기 첨삭에 빈칸 복원 단계 도입.** 첨삭 결과를 열면 `🔤 원어민 문장 되찾기`가 **먼저** 뜨고, 나머지 첨삭(TOEFL·점수·문법·교정문·모범 답안·표현·Q&A)은 `#writing-feedback-rest`에 숨겨 두었다가 `확인하기`/`⏭ 건너뛰기` 후에 연다. 빈칸의 출처는 `[[ ]]` 마커가 아니라 **`native_expressions` 배열**이다 — `[[ ]]`는 표시용이라 뜻·레벨이 없어 복습 카드를 만들 수 없지만 `native_expressions`는 `{expression, meaning, example, level, non_literal}`을 갖고 있어 **AI 추가 호출·추가 토큰 0으로** 인출 실패 청크가 그대로 덱 카드가 된다. 이를 위해 프롬프트 6번 항목을 "주제 관련 표현"에서 "**native_answer에 그대로 등장해야 하는** 표현"으로 좁혔다(범위가 좁아져 토큰은 오히려 줄었다). 도메인은 순수 함수 `features/writing/cloze.js`(`buildCloze`/`checkBlank`/`countBlanks`/`normalize`, 단어 경계·[[ ]] 제거·표현 1회 소비)+테스트, 화면은 `cloze-ui.js`. 첫 글자 힌트(단어마다 첫 글자)를 주는 이유는 인출이 아예 불가능하면 학습이 일어나지 않기 때문. **틀리거나 비운 빈칸의 표현은 고르게 하지 않고 자동으로 덱에 넣는다**(인출 실패 항목이 학습 가치가 가장 크다) — 2026-07-19 "표현은 개별 `➕`로 유저가 고른 것만 추가" 결정을 **인출 실패분에 한해** 예외로 둔 것이고, 나머지 표현은 그대로 유저가 고른다(`wireExpressionAdds`에 이미 담긴 표현을 `✓ 담김`으로 표시하는 4번째 인자 추가). 곁들여 `writing/ui.js`(312줄)를 `schema.js`(스키마 상수)·`qna.js`(첨삭 Q&A 상태)로 쪼개 185줄로 낮췄다 | 원어민 표현이 와닿지 않고 안 읽게 된다는 유저 문제. 근거: Involvement Load Hypothesis(Hulstijn & Laufer 2001 — 파지율 작문>빈칸>읽기), dictogloss 연구(Yu, Boers & Tremblay 2022 — 재구성 중 **스스로 인출에 성공한** 항목만 잘 남고 사후 정답 대조로 고친 항목은 아님), noticing the gap(Qi & Lapkin 2001, Adams 2003), Boers & Lindstromberg(하이라이팅만으로는 산출 이득 미미).
- 2026-07-29 | **표현에 `non_literal`(직역 불가) 태그 + 복습 예문을 자기 얘기로.** (1) 회화·글쓰기의 표현 스키마에 `non_literal: boolean`을 추가하고(required), 표현 목록·복습 목록·복습 카드에 `직역 불가` 배지(`dom.nonLiteralBadge`)를 단다. 덱 카드에는 `nonLiteral`로 저장(`store`는 `...item`을 펼치므로 스키마 변경 불필요). (2) 복습의 `✍️ 예문 만들기`를 `✍️ 내 이야기로 예문 만들기`로 바꾸고 placeholder·안내를 실제 내 경험·습관·계획을 쓰도록 유도, `comment`가 비개인적 문장을 지적하게 한 절 추가. 채점 루브릭(4축 절대 기준)은 건드리지 않는다 | (1) 한국어 직역으로 나오는 연어는 이미 쓸 줄 알고, 비직역(incongruent) 연어가 상급자도 오류율·반응시간이 높다는 연구 — 어떤 표현이 배울 가치가 있는지 눈에 띄게. (2) self-reference effect(*Psychon Bull Rev* 2025 — 자기 얘기 문장 생성이 번역 산출 대비 즉시 30.3% vs 14.3%, 1주 후 17.0% vs 10.2%). 둘 다 UI 문구와 필드 하나라 토큰 비용이 거의 없다.
- 2026-07-30 | **복습 과부하 해결 + 암기 효율 개선 — 하루 상한·오답 완화·leech·간격 분산·인터리빙.** 표현이 쌓이면 일일 공부량을 채워도 복습이 무한정 밀린다는 문제를 다섯 갈래로 나눠 고쳤다. (1) **하루 상한:** `srs/session.js`(순수 도메인 신규)의 `buildSession`이 신규/총량 2단 상한으로 큐를 자른다(`profile.dailyNewLimit` 5 / `dailyReviewLimit` 20 기본, 설정에서 가볍게·보통·집중 프리셋으로 변경). 잊을 위험이 큰 복습이 먼저 자리를 차지하고 남는 자리에만 신규를 들인다. 넘친 건 "대기 중 N개"로 표시만 하고 다음 날로 넘긴다 — 덱이 커지는 것 자체는 막지 않는다(Anki 운영법에서 신규 백로그는 부채가 아니라 저수지, 통제 대상은 하루 부담뿐). `todaysCounts`가 `records.quiz`를 서울 날짜로 세어 오늘 몫을 판정하므로 store에 카운터를 새로 두지 않았다(`quiz` 레코드에 `isNew` 추가). (2) **오답 완화:** `review()`가 `streak=0` 완전 리셋 대신 `LAPSE_DROP=4`칸 하락(120일→7일). 120일 카드가 한 번 틀렸다고 1일로 추락해 매일 되돌아오며 복습량을 부풀리던 것이 가장 큰 유입원이었다. `RETRY_MS`(10분 재도전)를 없애고 세션 말 **재도전 단계**로 옮겨, 카드 하나당 세션당 `review()` 1회·`quiz` 레코드 1건으로 고정했다(실패 카드가 레코드를 2건 남기던 버그도 해소). **재도전 성공은 간격을 올리지 않는다** — 같은 세션 안의 인출은 단기기억에서 나온 것이라 장기 파지의 증거가 아니다. 카드당 최대 2회(`RETRY_ALLOWANCE`). (3) **leech:** `lapses` 누적 8회(`LEECH_THRESHOLD`)에 `leech: true`. 몰래 없애거나 미루지 않고 그 자리에서 `[🗑 빼기 / 계속 볼래요]`를 묻고, "계속"은 `clearLeech`로 되돌려 8회 뒤 다시 묻는다. 목록에도 `⚠️ 잘 안 외워져요` 배지. (4) **간격 분산(fuzz):** `fuzzInterval`이 간격을 ±15% 흔든다(3일 미만은 제외). 간격이 결정론적이라 글쓰기 1회에 자동 투입된 5장이 영원히 같은 날 함께 돌아오던 문제 해소. 순수성 유지를 위해 `rand`를 인자로 받는다. (5) **인터리빙:** 표현 전체+단어 전체 단순 concat을 버리고 종류를 남은 개수 비율대로 섞는다. **인터리빙은 정렬 전 동순위 tie-break로 적용**한다 — 자르고 나서 섞으면 상한 안이 한 종류로만 채워지고, 섞고 나서 자르면 위험도 순서가 깨진다(테스트가 두 순서 모두 잡았다). 정렬은 절대 연체일이 아니라 **상대 연체도**(연체일/간격)로 — 1일 간격이 5일 밀린 것과 120일 간격이 5일 밀린 것은 위험도가 다르다. 곁들여 `masteryLabel`(갓 배운~매우 숙달된), 기억률 70%/95% 기준 상한 조정 힌트, 놓친 항목에 `✍️ 예문 만들기`를 주 버튼으로 강조를 넣었다. `toSeoulDate`는 복습·통계가 같은 하루 경계를 써야 해 `shared/date.js`로 옮겼고(stats.js는 재수출), `srs/ui.js`(388줄)를 `schema.js`·`labels.js`·`items.js`·`produce-ui.js`로 쪼개 251줄로 낮췄다. `dueCards`는 `buildSession`이 대체해 제거. **대체·수정하는 ADR:** 2026-07-17 "오답 시 처음으로(10분 뒤 재도전)" → 4칸 하락 + 세션 말 재도전 단계, 2026-07-19 "표현 복습을 버튼 기반 SRS로 개편"의 자가평가 흐름은 유지하고 그 위에 상한·leech·인터리빙을 얹음 | 근거: Anki 운영법(신규:복습 ≈ 1:10, leech 임계 8, fuzz로 복습 몰림 방지), FSRS(한 번의 실패로 기억 안정성이 0이 되지 않음, desired retention 85~90%가 스윗스팟 — 95%로 올리면 복습량 2배), Karpicke & Roediger 2007(장기 파지에서 확장간격이 등간격보다 낫지 않고 핵심은 첫 인출을 어렵게 하는 것 — 사다리는 복습량 이점 때문에 유지), Cepeda et al. 2008(최적 간격 ≈ 목표 파지기간의 10~20%, 현 사다리가 토플 3개월 기준 적정), Wilson et al. 2019 85% 규칙·Bjork desirable difficulties(기억률 힌트), Nakata & Suzuki 2019 등 인터리빙>블록킹, Yu/Boers/Tremblay 2022(스스로 인출에 성공한 항목만 잘 남는다 — 재도전을 승급 사유로 삼지 않은 근거), 말해보카(하루 목표 프리셋·숙련도 4단계·틀린 문제 재도전 — 유저 요청).

- 2026-07-30 | **글쓰기 빈칸 복원 난이도 완화 + 자동 덱 추가 철회.** (1) 빈칸을 표현 하나당 input 하나가 아니라 **띄어쓰기 단위로 쪼개** 표현이 몇 단어인지 보이게 하고, **글자 수만큼 잘린 밑줄**로 단어 길이를 알려 준다 — 등폭 글꼴 + `letter-spacing: 0.4ch`로 글자 진행폭을 1.4ch로 고정하고 밑줄을 같은 주기의 반복 그라디언트(1.4ch 중 1ch만 칠함)로 깔아 글자와 칸이 어긋나지 않게 했다(칸 폭 계산은 `cloze-ui.js`의 `SLOT`/`GAP`, 렌더는 `.cloze-input` — 한쪽만 바꾸면 어긋난다). 덕분에 타이핑하면 글자가 칸 위에 하나씩 얹혀 몇 글자가 비었는지 그 자리에서 보인다. 도메인은 `cloze.js`에 순수 함수 `wordsOf`/`checkWords` 추가+테스트, 힌트 버튼은 첫 글자만 첫 칸에 공개. 채점도 단어 단위로 표시해 어디가 틀렸는지 보이게 했다(표현 전체가 맞아야 정답인 판정 기준은 유지). (2) **인출 실패 표현을 자동으로 덱에 넣지 않는다** — 결과 요약 뒤에 열리는 표현 목록에서 `못 맞힘` 배지(`dom.expressionAddHTML`의 2번째 인자 `missed`)로 표시만 하고, 담을지는 기존 `➕`로 유저가 고른다. 이에 따라 `cloze-ui.js`의 `addToDeck` 호출과 `wireExpressionAdds`의 4번째 인자(`alreadyAdded`), 아무도 쓰지 않게 된 `countBlanks`를 제거했다. **수정하는 ADR:** 2026-07-29 "원어민 표현을 읽기에서 인출로"의 (a) 인출 실패분 자동 덱 추가 예외 → 철회(전부 유저 선택으로 통일), (b) 단어마다 첫 글자만 주는 힌트 → 단어 분리 + 잘린 밑줄로 길이 상시 표시, 첫 글자 힌트는 선택 | 표현 전체를 한 칸에 통째로 써야 해서 너무 어렵고, 관심 없는 표현까지 복습에 자동으로 쌓인다는 유저 불만. 인출 난이도는 desirable difficulty 범위 안에 있어야 학습이 일어난다(단서가 전혀 없으면 인출 시도 자체가 무너진다).

- 2026-07-31 | **복습을 자가평가에서 예문 빈칸 인출로 (말해보카식).** 표현/단어만 보여 주고 `🙂 기억나요 / 😵 까먹었어요`를 먼저 묻던 흐름을 없애고, **카드의 예문에서 그 표현만 빈칸으로 뚫어** 직접 채우게 한다. 단서는 예문 + **예문의 한국어 해석**이고, 막히면 `📖 뜻 알아보기`(뜻은 기본으로 감춰 둔다 — 뜻이 보이면 인출이 아니라 번역이 된다)와 `🔤 첫 글자 힌트`를 눌러 볼 수 있다. 빈칸은 글쓰기와 동일하게 **띄어쓰기 단위로 쪼갠 입력칸 + 글자 수만큼 잘린 밑줄**이라 몇 단어·몇 글자인지 보인다. **다음 복습 간격은 이 빈칸의 정답 여부가 정한다**(정답=사다리 상승, 오답·빈칸=`LAPSE_DROP` 하락 + 세션 말 재도전). 채점 뒤 `✍️ 내 이야기로 예문 만들기`(AI 첨삭)는 선택으로 남고 간격에는 영향을 주지 않는다. 구현: (1) 예문 해석을 담을 `example_ko`를 회화·글쓰기 표현 스키마와 사전 항목 스키마에 추가하고(기존 AI 호출에 필드 하나라 토큰 증가 미미) 카드에 `exampleKo`로 저장 — 해석이 없는 기존 카드는 해석 줄 없이 그대로 출제된다. (2) 빈칸 도메인 `writing/cloze.js` → **`shared/cloze.js`로 이동**(두 기능이 함께 쓰므로), 입력칸·채점 표시 렌더는 `shared/cloze-view.js`로 분리해 `SLOT`/`GAP`과 `.cloze-input` CSS 주기의 짝을 한 곳에 뒀다. (3) `srs/quiz.js`(순수 도메인+테스트)가 카드 하나를 문제로 바꾼다 — 예문에 표현이 **굴절형**으로 등장하므로(pay off → pays off) 원형으로 못 찾으면 첫 단어·마지막 단어의 흔한 굴절형까지 넓혀 찾고, 그래도 못 찾거나 예문이 없으면 **뜻을 단서로 표현 자체를 쓰는** 모드로 떨어진다. 화면은 `srs/quiz-ui.js`. 이에 따라 `produce-ui.js`의 `😵 잘못 생각했어요` 최종 확인과 `labels.meaningHTML`은 제거했다. **대체하는 ADR:** 2026-07-19 "표현 복습을 버튼 기반 SRS로 개편"(자가평가가 간격을 정함) → 빈칸 정답 여부가 정한다. 2026-07-19 "예문 만들기/예문 없이 넘어가기 선택"의 선택 구조는 채점 **뒤** 단계로 유지. 2026-07-30 상한·leech·재도전·인터리빙은 그대로 | 자가평가는 "기억난다고 착각"을 거르지 못하고 인출 강도가 약하다는 유저 불만. 근거: 인출 연습(testing effect)은 실제로 답을 산출할 때만 일어나고, 문맥 있는 빈칸이 단어-뜻 짝짓기보다 파지가 좋다(Involvement Load Hypothesis; 앞선 2026-07-29 ADR과 같은 계열). 말해보카의 예문 빈칸 형식(유저 요청).

- 2026-07-31 | **빈칸을 글자당 한 칸으로 — 잘린 밑줄 방식 폐기 + 스페이스바 이동 + 모바일 한 화면.** (1) 단어 하나에 input 하나를 두고 밑줄을 글자 수만큼 잘라 깔던 방식(2026-07-30 ADR의 `SLOT`/`GAP` + `.cloze-input` 1.4ch 그라디언트)을 **버리고, 한 글자 = `.cloze-cell` 하나**로 바꿨다. 칸이 물리적으로 떨어져 있어야 몇 글자인지 세지 않고도 보인다(밑줄 방식은 글자 수가 안 읽힌다는 유저 지적). 단어 안 간격 3px·단어 사이 13px로 단어 경계도 눈에 남는다. (2) **칸 사이 이동**을 `shared/cloze-view.js`의 `wireCells`가 붙인다: 한 글자 넣으면 다음 칸, **스페이스바 한 번이면 다음 단어 첫 칸**(단어 중간에서도), 빈 칸에서 백스페이스는 앞 칸을 지우며 이동, 좌우 화살표 이동, 포커스 시 `select()`로 덮어쓰기. 모바일 자동완성이 여러 글자를 밀어 넣는 경우를 대비해 `maxlength=1` 위에 마지막 글자만 남기는 방어를 뒀다. (3) **모바일 한 화면:** `.cloze-blank`를 `inline-flex; flex-wrap: wrap`으로 바꿔 **단어 단위로 줄바꿈**되게 하고(560px 이하에서 폰트·칸 폭 축소), 복습 화면은 줄바꿈된 칸을 가운데 정렬한다. 390px 폭 기준 22글자 표현("a blessing in disguise")까지 가로 스크롤 없이 한 화면에 들어간다. 값 읽기는 `readWords`(칸을 단어별로 이어 붙임)가 맡아 `checkWords`의 단어 단위 판정은 그대로다. **글쓰기·복습 양쪽에 동시 적용**(같은 모듈 공유, 유저 결정). 검증은 `chrome-headless-shell`을 CDP로 직접 몰아(playwright 미설치, node 내장 WebSocket 사용) 실제 앱에서 칸 개수·타이핑 이동·가로 스크롤 없음을 확인했다. **대체하는 ADR:** 2026-07-30 "글쓰기 빈칸 복원 난이도 완화"의 (1) 잘린 밑줄로 길이 표시 → 글자당 칸 | 밑줄로는 글자 수가 전달되지 않고, 빈칸이 화면 밖으로 넘쳐 휴대폰에서 가로 스크롤이 생긴다는 유저 불만.

- 2026-08-01 | **새 상단 탭 "📝 글쓰기 기본" — 토플 수준 모범 답안을 미리 써 두고 빈칸으로 채우는 연습, AI 호출 0회.** 지금까지 `✍️ 글쓰기`는 유저가 먼저 쓰고 AI가 첨삭하는 산출 중심 흐름이었는데, 그 앞 단계로 좋은 글의 구조·표현을 먼저 눈에 익히는 인풋 단계를 추가했다. `features/writing-basic/`에 논설문 템플릿 4종(선호 선택형·찬성반대형·양면평가형·정책제안형, `templates.js`)과 그 템플릿을 따르는 토플 만점 수준 모범 답안 12편(템플릿당 3편, `essays-*.js`→ `essays.js` 집계, 영문 250~300단어+문장별 한국어 해석)을 손으로 써서 정적 데이터로 미리 담았다. 각 표현 빈칸에는 난이도 1~3(초급=신호어 뼈대·중급=+핵심 연어·상급=+학술 어휘)을 미리 매겨 뒀고, `difficulty.js`의 순수 함수 `blanksFor(essay, difficultyId)`가 `level <= maxLevel` 필터로 골라 **상급 ⊃ 중급 ⊃ 초급 누적 관계**를 데이터 구조상 보장한다. 빈칸 판정·렌더는 새 로직을 만들지 않고 복습·글쓰기 인출에 쓰던 `shared/cloze.js`(`buildCloze`/`checkWords`)와 `shared/cloze-view.js`(글자당 칸 입력, 스페이스바 단어 이동, 첫 글자 힌트)를 그대로 재사용했다 — 유저 요청대로 복습·글쓰기 빈칸과 동일한 모양·조작감이다. 두 화면이 쓰던 짜깁기 로직(`weave`)은 `writing/cloze-ui.js`에서 `shared/cloze-view.js`의 `weaveHTML`로 승격해 공유한다. 못 맞힌 표현은 (다른 모든 기능과 동일하게) 자동으로 덱에 넣지 않고 `➕ 복습에 추가`로 유저가 고르며, 이때 `example`/`example_ko`는 그 표현이 실제로 등장한 문장과 그 해석을 넘겨 복습 예문 빈칸이 곧바로 성립하게 한다. 결과는 `records.writingBasic`(`essayId, template, difficulty, total, correct`)에 남기되 점수 통계(`stats.js` 달력·평균)에는 반영하지 않는다 — 이 연습은 산출 채점이 아니라 인지·인출 확인이라 절대 점수 체계에 억지로 끼워 넣지 않았다(유저 결정). `essays.test.js`가 12편 전체에서 "선언한 빈칸이 3개 난이도 모두에서 실제로 본문에 존재하는지"를 검증해, 손으로 쓴 표현 철자·중복 오류를 테스트로 잡는다. 다음 단계로 같은 틀(템플릿+빈칸)에 개정 토플 이메일 작성 문제를 얹을 계획(이번 범위 아님) | 어휘·구조를 먼저 인풋으로 접한 뒤 산출(`✍️ 글쓰기`)로 넘어가는 편이 학습 순서에 맞는다는 요구, 지문·빈칸이 전부 정적 데이터라 사전·번역기를 쓰지 않는 한 토큰 비용이 전혀 들지 않는다.

- 2026-08-01 | **글쓰기 기본 지문 12편 → 32편으로 확장(템플릿당 3편 → 8편).** 위 ADR의 12편에 템플릿별로 5편씩 손으로 더 써서 총 32편(선호 선택형/찬성반대형/양면평가형/정책제안형 각 8편)으로 늘렸다. 지문은 모두 `writingPrompts`(`writing/prompts.js`)에 이미 있던 질문 중 아직 안 쓰인 것에서 골랐다(새 질문 추가는 필요 없었음). 구조·검증 방식은 그대로 — `essays-*.js`에 append, `blanks[].level` 1~3로 난이도 배정, `essays.test.js`의 12→32 카운트만 갱신. 새로 쓰면서 반복적으로 걸린 실수 두 가지를 기록해 둔다: (1) `blanks`의 `expression`은 반드시 본문에 등장하는 그 형태 그대로(주어-동사 수 일치, 관사 포함)여야 한다 — "become accessible"이라 써도 본문이 "becomes accessible"이면 안 잡힌다. (2) `buildCloze`의 매칭은 순수 리터럴 부분 문자열이라 `"insulate ... from"`처럼 생략 부호로 여러 단어를 아우르려 하면 절대 안 잡힌다 — 항상 실제로 붙어 있는 연속된 단어만 표현으로 쓴다. 이 두 실수는 전부 `essays.test.js`의 "선언한 빈칸이 실제로 발견되는지" 검증에서 잡혔다 | 유저가 12편으로는 부족하다며 템플릿당 더 많은 지문을 요청.

- 2026-08-01 | **개정 토플(2026) "Write an Email" 유형 추가 — `✍️ 글쓰기`·`📝 글쓰기 기본` 양쪽에 토론형/이메일 모드 선택 도입.** 웹 조사(테스트석시드·마구시 등, 이번 CLAUDE.md 파일에 출처 명시 생략— PR 설명 참고)로 확인한 2026년 개정 토플 iBT Writing Task 2 "Write an Email" 사양을 반영했다: 7분, 0~5점, 상황(~90단어)+수신자+**반드시 다룰 3개 항목(bullet)**, 100~150단어, 채점은 항목 충족·공손한 어조가 핵심(기존 토론형의 논지 전개와는 축이 다르다). (1) **채점:** `shared/scoring.js`에 `email` 루브릭(task 35·accuracy 25·range 15·fluency 25 — 항목 충족과 어조를 무겁게) + `EMAIL_TASK_RUBRIC`(3개 항목 충족 여부가 task를 지배)·`EMAIL_REGISTER_NOTE`(수신자 관계에 맞는 공손함) 절대 밴드 추가. `scoring.test.js`는 `Object.keys(RUBRICS)`를 순회해 자동으로 검증을 포함했다. `writing/email.js`에 `toefl.js`를 복제한 0~5 홀리스틱 밴드(`TOEFL_EMAIL_BANDS`). `writing/schema.js`에 `EMAIL_REVIEW_SCHEMA` 추가 — 기존 `REVIEW_SCHEMA`와 대부분 같지만 `toefl_score` enum이 0~5이고, **`bullets_covered: [{bullet, covered, comment}]`**로 3개 항목 각각의 충족 여부를 AI가 명시적으로 판정한다. (2) **데이터:** `writing/email-prompts.js`에 상황 32개(요청·문의/불만·문제 제기/피드백·제안/사과·양해 각 8개, `{id, category, titleKo, recipient, situation, bullets}`) — AI 호출 없이 로컬에서 뽑는다(기존 `writingPrompts`와 같은 절약 원칙). `writing-basic/templates.js`에 `mode`(discussion|email) 필드를 추가하고 이메일 골격 템플릿 4종(요청·불만·피드백·사과, 각각 제목→인사→목적→항목3개→맺음말 골격)을 얹었다. `writing-basic/essays-email.js`에 이메일 모범 답안 8편(템플릿당 2편, 110~160단어, `bullets` 필드 추가)을 손으로 써서 `essays.js`에 합쳤다. (3) **화면:** 두 탭 모두 시작 화면에서 `💬 토론형 / ✉️ 이메일` 카드 2개를 매번 새로 고르게 했다(회화 카테고리 그리드와 같은 패턴 — 지난 선택을 기억해 건너뛰지 않는다). `✍️ 글쓰기`는 질문 카드 툴바에 `🔀 유형 바꾸기`를 추가해 연습 중에도 바로 되돌아갈 수 있게 했고, 이메일 모드에서는 상황·수신자·항목 3개를 보여 주고(`🧱 구조 제시` 버튼은 토론형 전용이라 숨김) 첨삭 결과에 `📋 요구 항목 충족 여부` 체크리스트를 추가로 연다. 긴 채점 시스템 프롬프트 2개(`discussionSystem`/`emailSystem`)는 `writing/review-prompt.js`(순수 문자열 빌더)로 분리해 `ui.js`를 300줄 제한 안에 유지했다. `📝 글쓰기 기본`은 모드 카드 → (그 모드 템플릿만 필터한) 난이도·지문 목록 → 연습 순으로, 연습 화면에 `🔀 유형 바꾸기`(모드 카드로)와 `← 목록`(같은 모드 목록으로)을 함께 둔다. 빈칸 판정·입력칸은 새 로직 없이 기존 `shared/cloze.js`/`cloze-view.js`를 그대로 재사용했다(이메일 본문도 문장 배열일 뿐이라 그대로 동작). 기록은 `records.writing`/`records.writingBasic`에 `mode` 필드만 추가해 저장하고(기존 기록은 `mode` 없음 = 토론형), `report.js`에 유형 라벨을 표시한다. `profile.basicMode`처럼 쓰이지 않는 상태는 추가하지 않았다(YAGNI — 매번 카드를 다시 고르므로 지난 선택을 저장해 둘 곳이 필요 없다). 검증은 `chrome-headless-shell`을 CDP로 직접 몰아 두 탭 모두 모드 전환·빈칸 채점·가로 스크롤 없음을 확인했고, 이메일 첨삭 1회를 실제로 호출해 `bullets_covered` 체크리스트와 0~5 배지가 정확히 렌더되는 것까지 확인했다(응답이 프롬프트와 무관한 내용이라 자동으로 0/5·전항목 미충족 판정이 나온 것도 루브릭이 의도대로 작동함을 보여준다) | 개정 토플에 새로 추가된 이메일 문제 유형을 앱에서도 연습할 수 있어야 한다는 요구. 근거 조사는 PR/커밋 설명에 링크로 남긴다.

- 2026-08-02 | **새 상단 탭 "📖 리딩" — VOA 지문 + 토플식 문제 + 요약·재진술.** 지문은 AI가 만들지 않는다: **GitHub Actions(`.github/workflows/voa.yml` 매일 05:00 KST)가 VOA Learning English RSS 6개 카테고리를 돌며 새 기사를 모아 `public/data/reading/`에 커밋**하고, 기존 `pages.yml`이 그 커밋을 배포하면 앱은 same-origin으로 읽는다. 이렇게 한 이유는 VOA가 CORS 헤더를 주지 않아 브라우저가 RSS·기사를 직접 못 읽기 때문이다(`curl -H "Origin: ..."`로 확인). CORS 프록시는 CSP에 외부 호스트를 열어야 하고 제3자 서비스에 의존하게 돼 버렸다. VOA는 전체가 퍼블릭 도메인이라 본문 저장·가공에 제약이 없다. 파싱은 `scripts/voa-parse.js`(순수 함수+테스트) — **본문 문단은 class 없는 `<p>`**라는 한 가지 규칙으로 공유·인쇄·댓글 버튼과 플레이어 자리표시자가 전부 걸러지고, `<p><strong>…</strong></p>`는 소제목, `____` 줄 이후는 VOA가 붙여 주는 어휘 풀이로 나눈다. mp3 링크도 함께 저장한다(나중 리스닝을 BBC 대신 VOA로 하면 저작권 제약이 사라진다 — 이번 범위 아님). 카테고리당 12편만 유지하고 350~1200단어만 받는다. **주의: VOA 피드 목록 페이지의 라벨은 실제 피드와 한 칸 밀려 있다** — 피드를 바꿀 땐 반드시 받아서 `<channel><title>`로 확인할 것(처음에 이 때문에 전부 잘못 매핑됐다). "America's Presidents" 피드는 U.S. History와 기사가 완전히 겹쳐 뺐다. `scripts/`와 `public/`은 배포 경계가 달라 카테고리 목록을 각자 들고 있고, 어긋남은 테스트가 잡는다. 곁들여 `.gitignore`의 `data/`가 `public/data/`까지 무시하고 있어 `/data/`(루트 한정)로 좁혔다.

  **학습 흐름은 한 화면 · 제출 한 번**(유저 결정): 지문 + 객관식 4~5문제 + 표현 빈칸 3~5개 + 문장 재진술 + 영어 요약을 함께 내고, 답하지 않은 문제는 **스킵**으로 처리한 뒤 종합 평가한다(실제 토플 리딩 섹션과 같은 구조). 문제 유형은 `features/reading/types.js`에 토플 iBT Reading 6유형(사실정보·부정사실·추론·수사적 의도·어휘·문장재진술)을 두고 매 지문 서로 다른 유형으로 출제한다. 빈칸은 새 로직 없이 `shared/cloze.js`/`cloze-view.js`를 그대로 재사용한다(글자당 칸·스페이스바 단어 이동·첫 글자 힌트 — 글쓰기·복습과 같은 조작감).

  **AI 호출은 딱 둘이고, 둘 다 좁혀 놨다.** (1) **문제 세트 생성 — 지문당 평생 1회.** `store.readingSets[articleId]`에 영구 캐시(사전 `dict`와 같은 방식, 백업·GitHub 동기화에 함께 실림). **객관식 정답 키를 함께 받아 두기 때문에 객관식·빈칸 채점은 100% 로컬**(`features/reading/score.js`, 순수 함수+테스트)이고 재도전은 AI 0회다. 빈칸의 영어 예문은 받지 않고 표현만 받아 `passage.js`가 지문에서 되찾는다(출력 토큰 절약). (2) **요약·재진술 채점 — 지문을 보내지 않는다.** (1)에서 받아 둔 `main_idea`+`key_points` 체크리스트만 보내고, `covered`는 항목 문구를 되돌려받지 않고 순서대로 불리언만 받는다(이 최적화만으로 채점 비용 $0.0370→$0.0317, 14%↓). **요약·재진술을 모두 비우고 제출하면 호출 자체를 하지 않는다(0원).** 채점 루브릭은 기존 4대 축을 쓰되 리딩 전용 절대 밴드 2개를 `shared/scoring.js`에 추가했다 — `READING_TASK_RUBRIC`(원문 충실도: 핵심 누락·사실 왜곡을 크게 감점하되 사소한 세부 생략은 감점하지 않음), `READING_RANGE_RUBRIC`(range를 "어휘가 화려한가"가 아니라 **원문을 베끼지 않고 자기 말로 바꿨는가**로 재정의 — 베껴 쓰기가 이 축의 최대 감점 사유). 배점 task 40·accuracy 20·range 20·fluency 20.

  **실측 비용**(헤드리스 크롬으로 실제 호출, `usage`에서 읽음): 문제 생성 ≈ **$0.039**(지문당 평생 1회) · 요약+재진술 채점 ≈ **$0.032**(매 시도) · 객관식·빈칸 채점과 캐시된 지문 재도전과 산출 스킵 = **$0** · 비교용 글쓰기 첨삭 1회 = **$0.052**. 즉 지문 한 편 완주는 처음 $0.071(글쓰기 첨삭의 1.4배), 두 번째부터 $0.032(0.6배), 이해 문제만 풀면 0원이다.

  기록은 `records.reading`에 남기고 통계·달력에 📖로 반영한다(유저 결정). 단 **요약·재진술을 모두 건너뛴 기록에는 `score`가 없어서**, `stats.summarize`/`dailyStats`가 점수 없는 기록을 평균에서 빼도록 방어했다. 번역기는 리딩에서 **-10점**(`TRANSLATOR_PENALTY.reading`) — 요약을 번역기로 쓰면 과제 자체가 사라지기 때문. 곁들여 사전·번역기 버튼을 `querySelectorAll` 일회성 바인딩에서 **이벤트 위임**으로 바꿨다(리딩은 툴바를 동적으로 그려 기존 방식으로는 동작하지 않는다). 표현은 다른 기능과 동일하게 자동으로 담지 않고 `못 맞힘` 배지만 달아 유저가 `➕`로 고른다.

  **구조화 출력 스키마 제약(발견):** Anthropic `output_config.format.json_schema`는 배열의 `minItems`(0·1 외), `maxItems`, `minimum`/`maximum`을 지원하지 않는다(`minItems values other than 0 or 1 are not supported` 오류). 개수는 `description`과 프롬프트로 지시하고, 4지선다·정답 인덱스 범위 같은 불변식은 `ai.js`가 응답을 받은 뒤 로컬에서 검증해 어긋난 문항을 버린다.

- 2026-08-02 | **리딩 지문 49편 전체에 문제 세트를 미리 만들어 레포에 실었다 — "지문 열 때 생성 → 유저 로컬 캐시" ADR을 깨지 않고 그 앞에 한 단계를 더했다.** 처음엔 Node 스크립트(`scripts/gen-reading-sets.js`)로 Anthropic API를 직접 호출해 자동 생성하려 했으나, **유저가 이를 중단시키고 AI(나 자신)가 직접 지문을 읽고 문제를 작성하도록 요청**했다 — 이미 이 세션 자체가 대화형으로 과금되는데 별도 API 호출로 중복 비용을 내는 게 낭비라는 이유였다(실행 중이던 스크립트는 즉시 `pkill`로 중단, 그때까지 API로 만든 13편은 검증 후 재사용). 이후 나머지 36편은 각 지문을 직접 읽고 `GENERATE_SCHEMA`와 동일한 형식으로 객관식 5문제(6유형 중 매번 다른 유형)·빈칸 4개·핵심 논지·요약 체크리스트를 손으로 작성해 `public/data/reading/sets/<기사id>.json`에 저장했다(AI API 호출 0회, 이 대화 자체의 추론만 사용). **검증은 `features/reading/sets.test.js`**(신규)가 맡는다 — 모든 세트를 로드해 근거 문장(`evidence`)·재진술 대상 문장(`restate_sentence`)이 실제로 해당 지문에 정확히 등장하는지, 빈칸 표현이 `passage.resolveBlanks`로 되찾아지는지(3개 이상), 유효 문제가 4개 이상인지(`score.validQuestions`, ai.js와 공유하도록 추출)를 검증한다 — `essays.test.js`(글쓰기 기본)와 같은 이유로, 손으로 쓴 데이터라도 레포에 실려 배포되는 이상 테스트가 필요하다. 이 검증 과정에서 VOA 원문의 실제 버그를 하나 발견해 고쳤다: 사전 풀이 링크가 걸린 단어 뒤에 `<a>` 태그를 공백으로 치환하며 구두점 앞에 불필요한 공백이 남는 문제(`"smoggy , said"`)였다 — `scripts/voa-parse.js`의 `stripTags`에 구두점 앞 공백 제거 정규식을 추가하고(회귀 테스트 포함), 이미 커밋된 49편 전체를 원본 URL로 재수집해 텍스트를 정리했다(기사 선택은 그대로, 본문만 재처리). **로딩 순서 변경(`features/reading/ai.js`):** ①이 유저가 이미 생성해 둔 캐시(`store.readingSets`) → ②레포에 실린 기본 세트(`catalog.loadShippedSet`, `data/reading/sets/<id>.json`을 same-origin fetch, 404는 정상 상황으로 처리) → ③그래도 없으면 그때 생성. ②를 찾으면 ①에도 저장해 두 번째 방문부터는 fetch조차 없다. 헤드리스 브라우저로 49편 전부를 실제로 열어 AI 비용이 정확히 $0임을 확인했다. 결과적으로 **지금 있는 지문 49편은 어떤 유저가 처음 열어도 영원히 AI 호출 0회**이고, 이후 GitHub Actions가 매일 새로 들여오는 지문(현재 VOA RSS 피드 특성상 카테고리별 12편 상한까지 자연히 채워짐, 지금은 science 9·health 11·education 11·history 9·arts 6·society 3편)에는 종전대로 처음 여는 유저가 부담한다. | 유저가 이미 이 대화에 지불하고 있는데 별도 API 과금까지 겹치는 건 낭비라는 실용적 판단. 검증 테스트를 먼저 만들어 두었기 때문에 사람이 손으로 쓴 데이터도 근거 문장 인용 오류(따옴표 불일치·문단 경계 등) 여러 건을 그 자리에서 잡아낼 수 있었다.

- 2026-08-02 | **리딩 생성 전 비용 확인창 도입 — "레포에 자동 커밋해 공유" 아이디어는 보안상 기각.** 이전 ADR(같은 날, "49편 전체 문제 세트를 레포에 미리 실어")에 이어, 앞으로 새로 들어오는 지문도 유저가 열 때 생성된 세트를 **이 앱의 소스 레포(`Markias21/AndysEng`)에 자동 커밋해 모두와 공유**하는 방안을 검토했으나 유저가 직접 위험성을 지적해 기각했다: 브라우저가 쥔 공유 GitHub 토큰에 로그용 프라이빗 레포(`AndysEng_log`)뿐 아니라 **배포 중인 이 앱의 소스 레포**까지 쓰기 권한을 주면, GitHub Contents API는 경로 단위 제한이 안 되어(레포 전체에 쓸 수 있으면 `index.html`·`sw.js`·CSP 설정까지 다 건드릴 수 있음) 토큰 유출이나 코드 버그 하나가 "개인 학습 기록 손상"이 아니라 "실서비스 사이트 자체가 위험해짐"으로 격상된다 — `pages.yml`이 `main` 푸시를 즉시 배포해 리뷰 단계도 없다. 정적 PWA라 서버 쪽에서 권한 경계를 둘 방법도 없다(클라이언트 JS가 유일한 강제 수단인데 그게 바로 버그 날 수 있는 코드). **최종 결정: GitHub Actions는 여전히 지문 수집만 하고(AI 미사용, 기존과 동일), 유저가 지문을 열 때 세트가 없으면 그 유저의 로컬 store에만 생성해 둔다**(기존 동작 그대로, `store.readingSets`는 이미 유저 개인 GitHub 로그 동기화에 실려 있어 그걸로 충분). 레포에 미리 채워 두고 싶으면 여전히 `scripts/gen-reading-sets.js`를 사람이 손으로 돌린다.

  대신 **생성이 필요한 시점에 예상 비용을 `confirm()`으로 안내하고 확인받은 뒤에만 호출**하도록 `features/reading/ai.js`를 쪼갰다: `readySet(article)`(유저 캐시→레포 기본 세트만 확인, AI 호출 없음, 없으면 null) / `estimatedCost(article, level)`(시스템 프롬프트+지문 글자 수를 토큰으로 환산해 대략치 산출, `shared/usage.js`에 새로 둔 `estimateUsd` 재사용) / `generateSet(article, level)`(실제 호출, 비용이 드는 유일한 지점). `practice-ui.js`가 `readySet` → 없으면 `confirm("...약 $X이 들어요. 계속할까요?")` → 확인 시에만 `generateSet`을 부르는 순서로 오케스트레이션한다(취소하면 목록으로 복귀, 비용 0). 헤드리스 브라우저로 세 경우(캐시 있음/취소/확인) 모두 실제 확인 — 캐시 있는 지문은 확인창 자체가 안 뜨고, 취소하면 AI 비용 $0, 확인하면 그때만 과금됨(추정 $0.032 vs 실비용 $0.0257로 안전한 방향의 오차). | 유저가 "PAT 보안 위험 아니냐"고 직접 문제 제기 — 공유 토큰의 피해 범위를 비공개 로그에서 실배포 사이트로 넓히는 건 이 프로젝트의 신뢰 모델(친구들끼리 공유하되 최악의 경우를 제한)을 깨는 일이라 판단.

- 2026-08-02 | **리딩 핵심논지 체크리스트에 항목별 근거 설명 추가.** `covered`를 불리언 배열(`[true, false, ...]`)에서 `{covered, comment}` 객체 배열로 바꿔, 항목마다 "왜 충족/미충족인지"를 한글 한 문장으로 함께 받는다(`schema.js` `GRADE_SCHEMA`, `prompts.js` `gradeSystem` 1번 항목, `result-ui.js` `coveredHTML`). 글쓰기 이메일 모드의 `bullets_covered`(`{bullet, covered, comment}`, 2026-08-01 ADR)와 같은 모양이라 새 패턴이 아니다 | 유저 피드백: "요약·재진술 채점 해설이 리딩의 객관식 문제 해설(정답 표시+근거 문장 인용+한글 이유)만큼 자세하지 않다." 체크리스트가 ✅/❌만 보여주고 왜 그런지 설명이 없던 것을 지적. 토큰 증가는 항목당 한 문장 수준이라 미미하다고 판단.

- 2026-08-02 | **글쓰기·리딩 요약/재진술에 타이핑 시작 타이머 추가.** 입력창에 첫 글자를 치는 순간부터 경과 시간을 세어 입력창 바로 아래에 보여주는 `shared/typing-timer.js`(`attachTypingTimer`, 순수 DOM 유틸)를 신설했다. 글쓰기는 `#writing-input` 아래 `#writing-timer` 하나, 리딩은 `#reading-restate`·`#reading-summary` 두 입력창이 있어 **어느 한쪽에든 먼저 타이핑을 시작하면 공유된 하나의 시작 시각을 두 타이머 표시 모두에 반영**한다(요약·재진술을 하나의 산출 과제로 보고 별도로 재지 않음). 제출 시 `stop()`으로 멈추고(경과 시간은 그대로 보여줌), 글쓰기는 다음 질문으로 넘어갈 때 `reset()`으로 숨기고 지운다(리딩은 문제 세트 자체가 매번 새로 렌더되어 입력창과 함께 자연히 사라진다). 기존 리딩 읽기시간 타이머(`#reading-timer`, 화면 진입 시각 기준)와는 독립된 별개 타이머다. 헤드리스 브라우저(`chrome-headless-shell`을 CDP로 직접 몰아 확인)로 글쓰기·리딩 양쪽에서 타이핑 전 숨김 → 첫 입력 시 표시 → 리딩 두 입력창 공유 시작 → 글쓰기 새 질문 시 리셋까지 확인 | 유저 요청: 산출(요약·재진술, 에세이)에 걸리는 시간을 스스로 체감하고 싶다는 요구.

- 2026-08-02 | **새 상단 탭 "🎧 3분 학습" — 짧은 리스닝+리딩 겸용, AI 완전 미사용 MVP.** `만들고 싶은 기능.md`(리스닝 미착수 항목의 "필요한 고려사항")에 리딩·리스닝 둘 다 지문이 길어 1~3분짜리 짧은 버전을 원한다는 메모가 있었다. 조사 결과 리딩 파이프라인이 이미 VOA 기사마다 mp3 오디오 링크(`audio` 필드)를 저장해 두고도 화면에서 전혀 안 쓰고 있었고, VOA에는 원래 짧은 프로그램(Words and Their Stories — 관용구 위주 주간 쇼, ~2~3분)이 텍스트+오디오를 함께 제공해 **짧은 리딩과 짧은 리스닝을 같은 콘텐츠 하나로 동시에 해결**할 수 있었다. 후보였던 News Words(어휘 위주)는 실제 확인 결과 비디오 시리즈라 본문 div·mp3가 없어(영상만 있음) 제외했다. **수집(신규, 리딩과 분리):** `scripts/fetch-voa-listening.js`가 `scripts/voa-parse.js`의 `LISTENING_CATEGORIES`(zone 987, feed `zmypyl-vomx-tpeyry_` — VOA rssfeeds 페이지에서 라벨·zone·feed가 한 `<li>` 블록 안에 함께 있어 이번엔 이전 경고(라벨-피드 밀림)에 걸리지 않았고, `<channel><title>`로도 재확인)를 돌려 `parseArticle`/`parseFeed`를 그대로 재사용하고, 분량 기준만 다르게 잡는다(150~600단어, 실측 300~500단어대). **오디오 없는 기사는 버린다**(이 기능의 핵심이 듣기이므로). `public/data/listening/`에 저장, `.github/workflows/voa.yml`이 리딩과 같은 커밋에 함께 수집. **채점(신규, AI 미사용 — 위시리스트 원안 "딕테이션은 AI 사용 X"과 일치):** `features/listening/dictation.js`(순수 함수)가 난이도별(초급=6글자 이상 내용어만·중급=내용어 전부·상급=불용어 포함 전부)로 빈칸을 고른다. **`shared/cloze.js`의 `buildCloze`는 재사용하지 않았다** — 표현 하나가 지문 전체에서 딱 한 번만 빈칸이 되는 텍스트 검색 방식이라, the/a처럼 흔한 단어가 한 문장에 여러 번 나오면 첫 등장만 잡히고 나머지는 유실된다(상급 = 전부 빈칸에서 특히 문제). 대신 위치를 아는 토큰을 직접 잘라 같은 출력 shape(`{translation, parts, blanks}`)을 만들어 `shared/cloze-view.js`(글자당 칸, 스페이스바 이동, 첫 글자 힌트)는 그대로 재사용했다. 채점은 `checkBlank` 재사용, 정답 여부만 로컬로 판정(0~100 AI 점수 없음). **화면:** 카테고리가 하나뿐이라 리딩과 달리 카테고리 그리드 없이 목록→연습 2단계, 연습 화면은 `<audio controls>` + 난이도 picker(`diff-picker`, 글쓰기 기본과 같은 스타일) + 받아쓰기 폼. **기록:** `records.listening`(신규, `store.js` `RECORD_KINDS`에 추가) `{articleId, category, difficulty, total, correct}` — score 없음. `stats.js` `dailyStats`는 `listenCount`만 세고(활동 카운트) 점수 평균·캘린더 등급 색칠에는 반영하지 않는다(무리하게 S~F 체계에 끼워 넣지 않음). 이해도 객관식(AI 생성)은 이번 범위 밖 — 사용자가 "받아쓰기만, AI 완전 미사용"을 선택. 헤드리스 브라우저로 목록·오디오 재생·난이도 전환(빈칸 수 936→1335 확인)·타이핑·채점·기록 표시·모바일 360px 무가로스크롤까지 실제 확인 | 사용자가 "리스닝 기능을 만들고 싶다"며 아이디어를 요청했고, 이어 "하는 김에 짧은 리딩도 가능한가?"라고 물어 콘텐츠 통합을 확정. AI 미사용으로 비용 0원인 채로 리딩·리스닝 두 가지 오래된 요청을 동시에 해소.
